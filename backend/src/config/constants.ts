// Pagination
export const PAGINATION_MAX = 100;
export const PAGINATION_DEFAULT_CHECKLISTS = 50;
export const PAGINATION_DEFAULT_USERS = 100;
export const NOTIF_PAGE_SIZE = 20;

// Batch operations
export const MARK_VIEWED_BATCH_SIZE = 25;
export const MARK_VIEWED_MAX = 500;

// WebSocket connections
export const CONNECTION_TTL_MINUTES = 30;

// Image upload
export const ALLOWED_IMAGE_TYPES = ['image/jpeg', 'image/png', 'image/webp', 'image/heic', 'image/heif'];
export const MAX_IMAGES_PER_ITEM = 20;
export const MAX_IMAGES_PER_CHECKLIST = 200;
export const MAX_IMAGE_FILE_SIZE = 10 * 1024 * 1024; // 10MB
export const MAX_IMAGES_PER_UPLOAD = 10;
export const MAX_IMAGE_URL_BATCH = 50;

// Template lifecycle
export const TEMPLATE_DELETE_RETENTION_DAYS = 30;
